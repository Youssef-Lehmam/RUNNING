import { FieldModel, FieldType } from "../base";
export declare type ObjectFieldKey = string | number | symbol;
export declare type ObjectFieldModel = {
    [s: ObjectFieldKey]: FieldModel<unknown>;
};
/**
 * Object field.
 *
 * @example
 * ```ts
 * const schema = fields.object({
 *      a: fields.number()
 * });
 *
 * schema.create({
 *      a: 1
 * });
 * ```
 */
export declare class ObjectField<T extends ObjectFieldModel> extends FieldModel<ObjectFieldType<T>> {
    readonly model: T;
    constructor(model: T);
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
export declare type ObjectFieldType<T extends ObjectFieldModel> = {
    [K in keyof T]: FieldType<T[K]>;
};
