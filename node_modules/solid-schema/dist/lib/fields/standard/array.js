"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ArrayField = void 0;
const base_1 = require("../base");
/**
 * Array field.
 *
 * @example
 * ```ts
 * const schema = fields.array(fields.string());
 *
 * schema.create(["hello", "world"]);
 * ```
 */
class ArrayField extends base_1.FieldModel {
    constructor(model) {
        super();
        this.model = model;
    }
    validate(value, key = "value") {
        if (!Array.isArray(value)) {
            throw new TypeError(`'${key}' must be an 'array'`);
        }
        value.forEach((val, i) => {
            this.model.validate(val, `Index ${i}`);
        });
        return true;
    }
    get name() {
        return `ArrayField<${this.model.name}>`;
    }
    get type() {
        return `${this.model.name}[]`;
    }
}
exports.ArrayField = ArrayField;
