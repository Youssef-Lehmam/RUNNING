import { FieldModel } from "../base";
import { ObjectFieldKey } from "./object";
/**
 * Record field.
 * Equivalent of `Record<K, V>` in TypeScript.
 *
 * @example
 * ```ts
 * const schema = fields.record(fields.string(), fields.number());
 *
 * schema.create({
 *      a: 2,
 *      b: 1
 * });
 * ```
 */
export declare class RecordField<T extends ObjectFieldKey, U> extends FieldModel<Record<T, U>> {
    readonly key: FieldModel<T>;
    readonly value: FieldModel<U>;
    constructor(key: FieldModel<T>, value: FieldModel<U>);
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
