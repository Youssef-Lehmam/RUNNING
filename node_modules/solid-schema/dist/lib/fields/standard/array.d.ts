import { FieldModel } from "../base";
/**
 * Array field.
 *
 * @example
 * ```ts
 * const schema = fields.array(fields.string());
 *
 * schema.create(["hello", "world"]);
 * ```
 */
export declare class ArrayField<T> extends FieldModel<T[]> {
    readonly model: FieldModel<T>;
    constructor(model: FieldModel<T>);
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
