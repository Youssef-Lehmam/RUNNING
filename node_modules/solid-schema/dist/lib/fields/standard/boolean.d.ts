import { FieldModel } from "../base";
/**
 * Boolean field.
 *
 * @example
 * ```ts
 * const schema = fields.boolean();
 *
 * schema.create(true);
 * ```
 */
export declare class BooleanField extends FieldModel<boolean> {
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
