import { FieldModel } from "../base";
/**
 * Number field.
 *
 * @example
 * ```ts
 * const schema = fields.number();
 *
 * schema.create(1);
 * ```
 */
export declare class NumberField extends FieldModel<number> {
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
