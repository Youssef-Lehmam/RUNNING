import { FieldModel } from "../base";
/**
 * Any field.
 * Allows any value.
 *
 * @example
 * ```ts
 * const schema = fields.any();
 *
 * schema.create(null);
 * ```
 */
export declare class AnyField extends FieldModel<any> {
    validate(_value: unknown, _key?: string): true | never;
    get name(): string;
    get type(): string;
}
