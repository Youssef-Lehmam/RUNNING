"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AndField = void 0;
const object_1 = require("../standard/object");
/**
 * And field.
 * Combines multiple object fields.
 *
 * @example
 * ```ts
 * const schema = fields.and(fields.object({
 *      a: fields.string(),
 *      b: fields.number(),
 * }));
 *
 * schema.create({
 *      a: "hello",
 *      b: 1
 * });
 * ```
 */
class AndField extends object_1.ObjectField {
    constructor(...models) {
        super(models
            .map((x) => x.model)
            .reduce((p, c) => {
            return Object.assign(p, c);
        }, {}));
        this.models = models;
    }
    get name() {
        return `AndField<${this.type}>`;
    }
}
exports.AndField = AndField;
