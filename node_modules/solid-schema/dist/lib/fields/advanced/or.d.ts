import { FieldModel } from "../base";
/**
 * Or/Union field.
 * Union (` | ` in TypeScript) between multiple fields.
 *
 * @example
 * ```ts
 * const schema = fields.or(fields.number(), fields.constant("hello" as const));
 *
 * schema.create(1);
 * schema.create("hello");
 * ```
 */
export declare class OrField<T extends readonly FieldModel<unknown>[]> extends FieldModel<{
    [M in keyof T]: T[M] extends FieldModel<infer U> ? U : never;
}[number]> {
    models: T;
    constructor(...models: T);
    validate(value: unknown, key?: string): true | never;
    get name(): string;
    get type(): string;
}
